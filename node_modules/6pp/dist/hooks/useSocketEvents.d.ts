import { Socket } from "socket.io-client";
import { SocketEventHandlers } from "../types/hooks.types.js";
declare const useSocketEvents: <T extends Record<string, any[]>>(socket: Socket, handlers: SocketEventHandlers<T>) => void;
export { useSocketEvents };
